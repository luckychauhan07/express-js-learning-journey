<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Home Details Page</title>
        <link rel="stylesheet" href="/output.css" />
    </head>

    <body
        class="bg-gray-100 min-h-screen flex flex-col items-center justify-start p-6"
    >
        <!-- Top Heading -->
        <h1 class="text-4xl font-extrabold text-gray-900 mb-4">
            Welcome to the Home Details Page
        </h1>

        <!-- Details Card -->
        <div class="w-full max-w-2xl bg-white p-8 rounded-2xl shadow-xl">
            <h2 class="text-2xl font-semibold text-gray-800 mb-6 text-center">
                Your Selected Home Details
            </h2>

            <li class="list-none space-y-4">
                <!-- Home Name -->
                <h3 class="text-xl font-bold text-gray-700">
                    <%= home.houseName %>
                </h3>

                <!-- Price -->
                <p class="text-gray-600 text-base">
                    <span class="font-semibold text-gray-800"
                        >Charge per night:</span
                    >
                    $<%= home.price %>
                </p>

                <!-- Location -->
                <p class="text-gray-600 text-base">
                    <span class="font-semibold text-gray-800">Location:</span>
                    <%= home.location %>
                </p>

                <!-- Rating -->
                <p class="text-gray-600 text-base">
                    <span class="font-semibold text-gray-800">Rating:</span>
                    <%= home.rating %> ⭐
                </p>

                <!-- Image -->
                <% if (home.photoUrl) { %>
                <div class="mt-6">
                    <img
                        src="<%= home.photoUrl %>"
                        alt="<%= home.houseName %>"
                        class="rounded-xl object-cover w-full h-60 border border-gray-200 shadow-sm"
                    />
                </div>
                <% } %>
            </li>
        </div>
    </body>
</html>
